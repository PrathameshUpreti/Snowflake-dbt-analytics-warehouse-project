version: 2

models:

  - name: dim_customers
    columns:
      - name: customer_id
        tests:
          - not_null
          - unique

  - name: dim_restaurants
    columns:
      - name: restaurant_id
        tests:
          - not_null
          - unique

  - name: dim_riders
    columns:
      - name: rider_id
        tests:
          - not_null
          - unique

  - name: fct_orders
    columns:
      - name: order_id
        tests:
          - not_null
          - unique

      - name: amount
        tests:
          - not_null
